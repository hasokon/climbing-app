import 'package:flutter/material.dart';

Widget Setting(BuildContext context) => Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          new Text(
            'Setting',
            style: Theme.of(context).textTheme.display1,
          ),
        ],
      ),
    );
