import 'package:flutter/material.dart';

Widget ClimbingChart(BuildContext context) => Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          new Text(
            'Chart',
            style: Theme.of(context).textTheme.display1,
          ),
        ],
      ),
    );
